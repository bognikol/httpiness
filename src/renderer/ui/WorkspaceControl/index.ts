/**
 * Workspace control represents everything on the UI right from the
 * RequestBrowser. It contains, among other things: container where workspace
 * items are shown (workspace items can be either HttpReqt or HttpAuth instances),
 * MacrosControl (which is used for handling parameters), Console (with execution
 * details) and WelcomeControl.
 */

export { WorkspaceControl } from "./WorkspaceControl";
